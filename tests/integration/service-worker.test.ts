/**
 * @file tests/integration/service-worker.test.ts
 * @description Tests d'intégration du Service Worker.
 *
 * Squelette — les tests E2E du service worker nécessitent playwright-crx.
 * TODO(P4-SW) : implémenter avec playwright-crx qui charge l'extension réelle.
 */

import { describe, it, expect } from 'vitest';

describe('Service Worker — intégration', () => {
  it('TODO(P4-SW) : MessageRouter rejette silencieusement les messages invalides', () => {
    // Test avec chrome-mock ou playwright-crx
    expect(true).toBe(true);
  });

  it('TODO(P4-SW) : QuotaManager intégré au routeur', () => {
    expect(true).toBe(true);
  });
});
