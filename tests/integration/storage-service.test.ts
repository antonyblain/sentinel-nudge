/**
 * @file tests/integration/storage-service.test.ts
 * @description Tests d'intégration du StorageService.
 *
 * Squelette — les tests d'intégration IndexedDB nécessitent un environnement
 * avec IndexedDB disponible (fake-indexeddb ou environment réel).
 * TODO(P4-STORAGE) : implémenter avec fake-indexeddb.
 */

import { describe, it, expect } from 'vitest';

describe('StorageService — intégration IndexedDB', () => {
  it('TODO(P4-STORAGE) : initDB crée les 5 stores', () => {
    // Ce test sera implémenté avec fake-indexeddb en P4
    expect(true).toBe(true);
  });

  it('TODO(P4-STORAGE) : logEvent + getEvents cycle aller-retour', () => {
    expect(true).toBe(true);
  });

  it('TODO(P4-STORAGE) : purgeExpired supprime les enregistrements expirés', () => {
    expect(true).toBe(true);
  });
});
