# PROBLÈMES — Problèmes techniques rencontrés par Claude

Chaque problème est traité selon le cycle PDCA : consigné ici (Plan), résolu immédiatement (Do), vérifié (Check), intégré dans les fichiers de mémoire (Act).

| ID | Description | Contexte | Solution / Contournement | Action corrective permanente | Statut |
|----|-------------|----------|--------------------------|------------------------------|--------|
| P-001 | Lecture d'un fichier volumineux (24468 tokens) sans offset/limit, provoquant une erreur de dépassement du seuil de 10000 tokens | Lecture du CdC v1.0 (p1-cahier-des-charges-v1.0.md) pour intégrer les commentaires du Commanditaire | Relecture avec offset/limit par blocs de 200 lignes | Règle : tout fichier > 200 lignes doit être lu par blocs (offset+limit) ou via Grep ciblé. Ne jamais lire un fichier entier sans vérifier sa taille. Intégré dans LESSONS_LEARNED.md. | Résolu |
| P-002 | Tentative de commit Git sans vérifier la configuration user.name/user.email sur le poste | Premier commit sur un nouveau poste Windows après checkout du projet | Demande au Commanditaire de configurer Git manuellement | Règle : avant tout commit, vérifier git config user.name et user.email. Si absent, prescrire la configuration avant de tenter le commit. | Résolu |
| P-003 | git add sur un fichier déjà renommé (ancien nom inexistant), erreur pathspec | Commit du CdC v1.1 — le fichier v1.0 avait été renommé en v1.1 en amont | Utilisation de git rm sur l'ancien nom + git add sur le nouveau | Règle : avant toute commande git add, relire git status pour connaître l'état réel des fichiers. Ne jamais construire une commande à partir d'un état supposé. | Résolu |
| P-004 | Modification du SFD (M9 passphrase) sans mise à jour des fichiers .mmd et PNG associés | Ajout de la détection passphrase dans le texte du SFD mais oubli de la chaîne complète | Correction après signalement du Commanditaire : mise à jour .mmd + régénération PNG | Règle : toute modification d'un diagramme dans le SFD impose la mise à jour de 3 fichiers : 1) bloc Mermaid dans le SFD, 2) source .mmd dans diagrammes/sources/, 3) PNG régénéré dans diagrammes/images/. Vérifier aussi les impacts transversaux (matrice de traçabilité, compteurs CA). | Résolu |
| P-005 | Version `vite-plugin-web-extension` incorrecte dans le DAT : `^0.13.0` alors que le package npm est en v4.x (dernière : 4.5.1) | npm install échoue avec ETARGET "No matching version found" | Correction à `^4.5.0` dans package.json | Le DAT Annexe A doit refléter les versions réelles des packages npm. Vérifier npm view <pkg> versions avant de figer une version dans la doc. | Résolu — DAT à corriger |
| P-006 | Versions `@typescript-eslint/*` ^7.0.0 incompatibles avec ESLint ^9.0.0 (peer dependency conflict) | npm install échoue avec ERESOLVE peer dependency conflict | Passage à `@typescript-eslint/*` ^8.0.0 compatible ESLint 9 | Vérifier la matrice de compatibilité des peer dependencies avant de documenter les versions dans le DAT. | Résolu — DAT à corriger |
| P-007 | ESLint 9 ne supporte plus `.eslintrc.json` — exige `eslint.config.js` (flat config) | npm run lint échoue "couldn't find eslint.config" | Migration vers eslint.config.js au format flat config ESM | Le DAT §9.3 référence `.eslintrc.json` qui est obsolète depuis ESLint 9. Mettre à jour. | Résolu — DAT à corriger |
| P-008 | Pages HTML référencent `<script src="popup.js">` au lieu de `<script src="popup.ts">` | Vite build échoue "Could not resolve entry module" car Vite attend les extensions .ts pour les transformer | Remplacement .js → .ts dans les 4 fichiers HTML | Règle : dans un projet Vite + TypeScript, les scripts dans les HTML doivent référencer les fichiers .ts (Vite les transforme). | Résolu |
| P-009 | Vite ne trouve pas les entry points du manifest (pages) car root par défaut = racine projet, mais les fichiers sont dans src/ | Build échoue "Could not resolve entry module pages/popup/popup.html" | Ajout `root: resolve(__dirname, 'src')` dans vite.config.ts | Quand le manifest.json est dans src/, il faut configurer `root: 'src'` dans Vite pour que les chemins relatifs du manifest soient résolus correctement. | Résolu |
| P-010 | Manifest déclare `"service_worker": "background/service-worker.js"` mais Vite attend .ts | Second build échoue "Could not resolve entry module" pour le SW | Changement en `"service_worker": "background/service-worker.ts"` dans le manifest source | Le manifest source doit utiliser les extensions .ts ; vite-plugin-web-extension les compile en .js dans le dist/. Le DAT Annexe B doit refléter ceci. | Résolu — DAT à corriger |
| P-011 | jsdom non listé dans les devDependencies mais requis par Vitest (environment: 'jsdom') | npm run test échoue "Cannot find dependency jsdom" | Ajout jsdom en devDependency | Toute dépendance de test mentionnée dans la config (environment, plugins) doit être dans les devDependencies du package.json. Le DAT Annexe A doit lister jsdom. | Résolu — DAT à corriger |
| P-012 | SubtleCrypto (Web Crypto API) non disponible dans jsdom — 3 tests crypto échouent | Tests unitaires crypto-service.test.ts échouent sur crypto.subtle.generateKey() | Tests nécessitent un mock ou l'usage de @vitest/web-crypto ou globalThis.crypto polyfill | Documenter dans le DAT que les tests crypto nécessitent un polyfill/mock de SubtleCrypto car jsdom ne l'implémente pas. | Ouvert |
| P-013 | `type: "module"` manquant dans package.json — warning Node.js sur eslint.config.js | Warning "Module type not specified, reparsing as ES module" à chaque npm run lint | Ajout `"type": "module"` dans package.json | Pour un projet ESM (Vite, ESLint flat config), `"type": "module"` est obligatoire dans package.json. | Résolu |
| P-014 | Les Custom Elements (customElements.define) ne fonctionnent pas dans les content scripts Chrome MV3 (isolated world). document.createElement("sn-xxx") retourne HTMLUnknownElement, new Class() lance "Illegal constructor". | Développement des composants UI (overlays M2, M9, toast M7) en content script Chrome MV3 | Construire le DOM directement avec div + Shadow DOM attaché manuellement. Pas de customElements.define() dans les content scripts. | **Règle permanente** — ne JAMAIS utiliser customElements.define() dans un content script Chrome MV3. Toujours construire les composants UI via createElement + attachShadow. | Résolu |
| P-015 | 10+ échecs CI consécutifs sur GitHub Actions — Prettier format:check échoue sur des fichiers modifiés ou produits par des agents | Modifications manuelles (Edit) et production par agents délégués ne passent pas par Prettier. Le CI échoue systématiquement à l'étape "Vérification format Prettier". | `npx prettier --write` sur les 3 fichiers identifiés (typosquatting-targets.json, paste-detector.ts, message-validator.ts) | **Règle permanente** — Avant tout commit, exécuter `npm run format:check` (ou `npx prettier --write src/`) pour garantir le formatage. S'applique particulièrement après : (1) production par un agent délégué, (2) modification de fichiers JSON volumineux, (3) suppression/refactoring de code. Intégrer cette vérification dans le workflow pré-commit systématique : `npm run lint && npm run format:check && npm run build && npm run test`. | Résolu |
| P-016 | M7 silencieux lors des tests manuels — `handleFormSubmit` retourne sans log si `installation_salt` est absent de `chrome.storage.local`. Cause : soit onboarding incomplet (le sel est créé dans `onFirstInstall`), soit purge du storage via `chrome.storage.local.clear()` (comme recommandé par erreur dans le guide de tests). De plus, M7 est désactivé par défaut (opt-in RGPD via l'onboarding) — un message envoyé au SW serait skippé silencieusement. | Tests manuels M7 : rien ne se passe et aucun log dans la console SW | 1) Ajout d'un `console.warn` structuré dans `password-detector.ts:handleFormSubmit` quand le sel est absent (visibilité diagnostic). 2) Correction du guide de tests P4' : retrait de `chrome.storage.local.clear()` au profit d'une purge ciblée qui préserve `installation_salt` et `encryption_key_material`. 3) Ajout d'une commande DevTools dans le guide pour régénérer le sel manuellement + activer M7 dans la config sans passer par l'onboarding complet. | **Règle permanente** — Tout silent fail dans un handler ou un détecteur MV3 doit être doublé d'un `console.warn` structuré (timestamp + level + message + context). Les tests manuels doivent toujours prévoir la régénération des pré-requis storage (sel, clé, config) avant chaque session, pas une purge complète. Le guide de tests ne doit jamais préconiser `chrome.storage.local.clear()` sans warning explicite sur la perte du sel et de la clé de chiffrement. | Résolu |
