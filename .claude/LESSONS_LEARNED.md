# LEÇONS APPRISES — Amélioration continue

Ce fichier est alimenté en continu après chaque problème résolu (PDCA Act) et synthétisé lors de la rétrospective de fin de projet.

## Leçons

| ID | Date | Catégorie | Ce qui s'est passé | Ce qui a bien fonctionné | Ce qui a échoué | Amélioration appliquée |
|----|------|-----------|--------------------|--------------------------|-----------------|-----------------------|
| LL-001 | 2026-04-11 | Processus | Production de 3 fichiers distincts pour un même livrable (draft, v1.0, compléments) au lieu de maintenir un seul document | La grille d'analyse uniforme et la rigueur scientifique | La multiplication des fichiers a créé de la confusion pour le Commanditaire | Règle : mettre à jour le document en place pour des modifications mineures, ou incrémenter la version majeure et supprimer l'ancienne version. Ne jamais laisser plusieurs versions d'un même livrable coexister. |
| LL-002 | 2026-04-11 | Technique | Tentative de lecture d'un fichier de 24468 tokens (CdC v1.0, ~1500 lignes) sans offset/limit, provoquant une erreur de dépassement du seuil de 10000 tokens | Le fallback avec offset+limit par blocs de 200 lignes a fonctionné | La première tentative en lecture intégrale a échoué et gaspillé un appel outil | **Règle permanente** : tout fichier susceptible de dépasser 200 lignes doit être lu par blocs (offset+limit de 200 lignes max) ou via Grep ciblé sur les sections pertinentes. Avant de lire un fichier volumineux, estimer sa taille ou utiliser Grep pour localiser la section d'intérêt. |
| LL-003 | 2026-04-11 | Git | Tentative de commit sur un nouveau poste sans vérifier la configuration Git (user.name/user.email absents) | L'erreur a été identifiée rapidement grâce au message Git explicite | Le commit a échoué, nécessitant une action manuelle du Commanditaire et un second passage | **Règle permanente** : avant tout commit prescrit au Commanditaire ou exécuté par l'assistant, vérifier `git config user.name` et `git config user.email`. Si absent, prescrire la configuration en premier. Particulièrement important lors d'un changement de poste ou d'un premier checkout. |
| LL-004 | 2026-04-11 | Git | Commande `git add` construite à partir d'un état supposé des fichiers (ancien nom v1.0) alors que le fichier avait déjà été renommé en v1.1 | Le diagnostic de l'erreur pathspec a été rapide | L'erreur a gaspillé un appel et aurait pu être évitée | **Règle permanente** : toujours relire `git status` juste avant de construire les commandes git add/rm/commit. Ne jamais se fier à un état supposé — un fichier peut avoir été renommé, supprimé ou modifié entre-temps. |
| LL-005 | 2026-04-11 | Processus | Modification du SFD (ajout passphrase M9) sans penser à mettre à jour les fichiers .mmd sources et les images PNG. Le Commanditaire a dû signaler l'oubli. | La correction a été rapide une fois identifiée | La chaîne de documents dérivés n'a pas été suivie, créant une incohérence temporaire entre le SFD, les sources Mermaid et les images | **Règle permanente — Chaîne de modification documentaire** : toute modification du SFD qui touche un diagramme impose la mise à jour de toute la chaîne : (1) bloc Mermaid dans le .md, (2) fichier .mmd dans sources/, (3) PNG régénéré dans images/. Plus largement, avant de commiter une modification, toujours se poser la question : « quels autres fichiers sont dérivés de ou liés à ce que je viens de modifier ? » (matrice de traçabilité, compteurs CA, SESSION.md, BACKLOG.md). |
| LL-006 | 2026-04-11 | Processus | Le comité d'architecture P3 a produit 15 corrections (schéma password_hashes, domain_hash salé, périmètre chiffrement, rôles ARIA, etc.) qui modifient directement le SFD. Le DAT v1.1 a été produit avec les corrections, mais le SFD n'a PAS été mis à jour en v1.1. Le référent qualité a dû signaler 3 incohérences bloquantes lors de la revue globale. | Le référent qualité a bien détecté les incohérences lors de la revue de 2nd niveau | La rétro-propagation des décisions de comité vers les documents amont n'a pas été faite. Le SFD et le DAT décrivaient des schémas différents pour la même implémentation. | **Règle permanente — Rétro-propagation post-comité** : immédiatement après la rédaction du PV d'un comité, vérifier si les décisions prises modifient des spécifications de documents antérieurs (SFD, CdC). Si oui, mettre à jour ces documents AVANT de passer à la soumission. Ne jamais attendre la revue qualité pour détecter ces incohérences — c'est un échec de processus. Checklist : SFD impacté ? CdC impacté ? Diagrammes impactés ? RISQUES/BACKLOG/TECH_STACK impactés ? |
| LL-007 | 2026-04-11 | Technique | 9 anomalies rencontrées lors de l'initialisation du socle P4 : versions npm incorrectes dans le DAT (vite-plugin-web-extension ^0.13 n'existe pas → v4.5, @typescript-eslint ^7 incompatible ESLint 9 → ^8), format ESLint obsolète (.eslintrc.json → eslint.config.js flat config), HTML référençant .js au lieu de .ts (Vite exige .ts), root Vite manquant (src/), manifest avec .js au lieu de .ts, jsdom manquant, SubtleCrypto non dispo dans jsdom, package.json sans type:module. | Le diagnostic de chaque erreur a été rapide grâce aux messages d'erreur explicites | **Les versions documentées dans le DAT n'avaient pas été vérifiées contre npm.** Aucun `npm view <pkg> versions` n'a été exécuté en P3 pour confirmer l'existence des versions prescrites. | **Règle permanente — Vérification des versions npm en P3** : avant de figer une version dans le DAT (Annexe A), toujours exécuter `npm view <pkg> versions` pour confirmer que la version existe sur le registre npm. Vérifier aussi les peer dependencies entre les packages (ex: ESLint 9 exige @typescript-eslint ^8). Après la phase de développement P4, toujours faire un PDCA pour corriger les docs si les versions réelles diffèrent. |

| LL-008 | 2026-04-12 | Technique | Les 7 composants UI (overlays, toasts) utilisaient customElements.define() pour s'enregistrer comme Web Components. En content script Chrome MV3, customElements.define() s'exécute dans l'isolated world mais document.createElement() crée un HTMLUnknownElement. new Class() lance "Illegal constructor". Le toast M17 et l'indicateur M9 étaient invisibles/crashants. | La construction DOM directe (createElement + attachShadow + styles inline) fonctionne parfaitement. | 5 tentatives successives (registerXxx, window.customElements, try-catch, new Class) avant de comprendre que les Custom Elements sont fondamentalement incompatibles avec les content scripts MV3. | **Règle permanente** — pour les content scripts Chrome MV3, toujours construire les composants UI via createElement + attachShadow. Ne jamais utiliser customElements.define(). |

| LL-009 | 2026-04-13 | CI/CD | 10+ échecs CI consécutifs sur GitHub Actions à cause de Prettier (format:check). Fichiers modifiés par Edit ou produits par agents délégués sans passer par Prettier. Le Commanditaire a dû signaler le problème. | Les tests locaux (npm run test, npm run build) passaient toujours — la détection locale lint/format n'était pas systématique. | Aucune vérification format/lint n'était exécutée avant le commit. Les agents délégués produisent du code non formaté. L'enchaînement de pushs sans vérifier le statut CI a multiplié les échecs. | **Règle permanente — Checklist pré-commit** : avant chaque commit, exécuter dans l'ordre : `npm run format:check` → `npm run lint` → `npm run build` → `npm run test`. Si format:check échoue, exécuter `npx prettier --write src/` puis re-vérifier. **Ne jamais pusher un second commit sans avoir vérifié le statut du premier sur GitHub Actions.** S'applique particulièrement après production par un agent délégué ou modification de fichiers JSON. |

| LL-021 | 2026-04-17 | CI/CD | CI rouge depuis PR #4 (TACHE-061, 2026-04-16) propagée à PR #5 (TACHE-058) sans détection pendant 24h. 3 tests canary TC-M7-14 + boot-sequence TC-M7-14/15 échouaient sur Node 20 CI alors qu'ils passaient sur Node 24 local. Le Commanditaire a dû signaler « les commits récents échouent ». **La leçon LL-009 existait déjà et n'a PAS été appliquée**. Cause racine technique : `globalThis.crypto.subtle` non accessible dans le contexte VM Vitest/jsdom sur Node 20 (alors qu'il l'est sur Node 24). Le polyfill `setup.ts` échouait silencieusement (assignation sur getter jsdom sans setter). | La version Node local (24) diffère de la CI (20). Le rapport d'agent délégué annonçait « CI verte 239/239 » qui était en réalité « checklist locale verte » — sémantique ambiguë. Aucune vérification `gh pr checks` après push. | Divergence de versions Node + rapport ambigu + règle LL-009 non opérationnalisée = CI rouge silencieuse. La règle existait mais n'était pas systématique. | **Règle permanente double** (INSTRUCTIONS.md I-009 + I-010) : (1) après chaque `git push`, attendre ~60s puis exécuter `gh pr checks <numero>` et confirmer SUCCESS AVANT toute autre action ; (2) aligner strictement la version Node local ↔ CI (même major LTS), déclarer dans `package.json` (`engines.node`). **Fix technique appliqué** : `tests/setup.ts` utilise `Object.defineProperty` pour contourner le getter jsdom ; `canary-service.ts` et `crypto-service.ts` référencent `globalThis.crypto` explicite ; CI Node 20 → Node 22 LTS. **Mémoire auto** : `feedback_verification_ci_apres_push.md`. |

| LL-022 | 2026-04-17 | Processus | Parallélisation intensive d'agents Fabrique (jusqu'à 3 simultanés) sur une session de 90 min : 7 PR mergées via délégation en agents background avec isolation worktree. Forte accélération mais 3 incidents mineurs : (1) agents qui écrivent initialement dans le repo principal au lieu du worktree isolé — T-075 puis T-110 signalés l'incident avant correction (leçon à capitaliser dans les briefs), (2) collision potentielle sur `.claude/BACKLOG.md` si plusieurs agents y ajoutent des tâches en parallèle — résolu en centralisant l'ajout dans une seule PR, (3) difficulté à choisir entre "1 grosse PR combinée" vs "N petites PR parallèles" selon les zones touchées. | La parallélisation multiplie le throughput de 3× à 5× sur des tâches de même pattern (ex. chantier A audit ADR — M2 fait seul, M3+M5+M6 combinés). Les worktrees isolés `.claude/worktrees/agent-<id>` permettent des branches git parallèles sans collision de HEAD. | Sans vigilance sur les briefs (rappel `pwd` obligatoire avant Write) et le cleanup post-merge (worktrees + branches orphelines), le repo s'enlise en pollution git. Les zones partagées (`.claude/BACKLOG.md`, `src/shared/types/diagnostics.ts`) sont des points chauds de conflit si non centralisées. | **Pattern permanent — Parallélisation agents Fabrique** : (1) limiter à **2 agents simultanés max** en worktrees (au-delà l'isolation devient fragile), (2) imposer dans chaque brief : « vérifier `pwd` avant tout Write, CWD attendu `/c/Dev/sentinel-nudge/.claude/worktrees/agent-<id>` », (3) segmenter strictement les **zones autorisées** par agent (périmètre explicite), (4) pour des tâches de même pattern (ex. chantier A M3/M5/M6), préférer **1 agent combiné** plutôt que 3 parallèles, (5) centraliser toute modification de fichier partagé (`.claude/BACKLOG.md`, `docs/securite/referentiel-*.md`) dans **un seul agent à la fois**. |

| LL-023 | 2026-04-17 | Processus | Lors d'une session avec plusieurs agents Fabrique en background, la notification de complétion d'un agent peut arriver avec un retard significatif (observé +18 min sur T-114). Pendant ce temps, la PR est ouverte, CI verte, mais n'est pas mergée — le flux est bloqué. Le Commanditaire a détecté le problème visuellement avant que l'orchestrateur ne le voie. | Les notifications `task-notification` du runtime sont fiables à terme mais pas temps-réel. | S'appuyer uniquement sur les notifications automatiques = attente passive = latence perçue par le Commanditaire. | **Règle permanente — Check pro-actif agents background** : après avoir lancé 1+ agent en background, mettre en place un **Monitor dédié** qui scanne `gh pr list --state open --author @me` toutes les 60-120s, ou faire un check manuel `gh pr list` + `gh run list` toutes les 10-15 min pendant l'attente. Ne jamais supposer que l'absence de notification = agent toujours en cours. Cette règle est à appliquer **même quand un Monitor existe déjà** sur une autre PR (les Monitors ne sont pas transférables). |

| LL-024 | 2026-04-17 | Git | Après merge d'une PR issue d'un agent Fabrique avec `gh pr merge --delete-branch`, le worktree local `.claude/worktrees/agent-<id>` et la branche locale `worktree-agent-<id>` ne sont pas supprimés automatiquement. Accumulation progressive jusqu'à 4+ worktrees orphelins, bloquant les futurs `--delete-branch`. | `git worktree remove -f -f` permet de forcer le cleanup même quand le worktree est locked par le runtime. | Pas de cleanup automatique dans le cycle agent → PR → merge. Les artefacts git s'accumulent. | **Routine post-merge permanente** : après chaque `gh pr merge --delete-branch` d'une PR issue d'un agent Fabrique, exécuter en chaîne : `git worktree remove -f -f .claude/worktrees/agent-<id>` + `git branch --delete --force worktree-agent-<id>`. À défaut, batch périodique en fin de session : `git worktree list` pour identifier les worktrees pointant sur une branche mergée (ou absente de `origin`), puis cleanup groupé. |

| LL-025 | 2026-04-17 | Processus | **Panique prématurée et conclusion erronée d'incident** : j'ai interprété des fichiers modifiés dans le working tree du repo principal comme un incident d'isolation worktree de l'agent T-089+090+091, et j'ai tenté de « récupérer son travail » en créant une branche alternative. En réalité, l'agent fonctionnait parfaitement dans son worktree isolé et a pushé PR #25 avec succès 15 min plus tard. Les modifs que j'avais vues étaient soit un transitoire de migration, soit l'effet d'un checkout que j'avais fait moi-même. Le Commanditaire a dû me recadrer (« Je vois des tâches arrêtées et en échec, améliore toi »). | Le Commanditaire m'a signalé le problème rapidement. J'ai reconnu l'erreur et fait un diagnostic factuel complet avant toute action corrective. Aucun dommage réel (je n'ai pas eu le temps de pusher ma « récupération » doublon grâce au pré-commit qui a levé 1 test en échec — retard providentiel). | Diagnostic précipité : j'ai vu `git status` avec des modifs et j'ai conclu « agent n'a pas respecté l'isolation worktree » sans vérifier (a) si la branche feature de l'agent avait réellement zéro commit de lui (`git log -3` sur une branche non poussée montrait des commits de develop que j'ai confondu avec « branche vide »), (b) depuis combien de temps l'agent tournait (seulement 20 min sur une tâche combinée 3× prévue ~40 min). J'ai violé ma propre règle LL-023. | **Règle permanente — Diagnostic factuel AVANT action corrective** : face à toute anomalie perçue (fichiers modifiés inattendus, agent « silencieux », CI qui tarde) : (1) `gh pr list --state all --limit 10` pour voir toutes les PR, (2) `gh run list --limit 10` pour voir l'état CI, (3) `git log <feature-branch-agent> --oneline -10` pour voir les commits de l'agent sur SA branche (pas sur develop), (4) `git worktree list`, (5) **calculer le temps écoulé** vs estimation initiale. Ne **JAMAIS** tenter de « récupérer » un travail avant ces 5 checks. Si après 5 checks l'anomalie persiste, `SendMessage` à l'agent avant toute action destructive. |

| LL-026 | 2026-04-17 | Processus | Complément LL-025 : j'avais failli créer une PR alternative avec les 8 fichiers « récupérés » du working tree. Si j'avais été plus rapide avant le push de l'agent, j'aurais créé un doublon avec des commits divergents → conflit de merge assuré → pollution de la branche feature. Seul le pré-commit `npm run test` obligatoire (1 test en échec m'a ralenti suffisamment) a évité la catastrophe. | La règle LL-009/LL-021 de pré-commit obligatoire m'a mécaniquement ralenti et m'a évité une catastrophe. | J'aurais dû refaire les 5 checks de LL-025 après chaque action intermédiaire, pas seulement avant d'entamer la « récupération ». Ma boucle « récupérer travail » a tourné en parallèle de l'agent pendant 10 min alors qu'il finissait correctement. | **Règle permanente — Durée d'attente minimale avant action corrective** : avant toute « récupération » de travail, respecter une durée d'attente minimale selon le profil de l'agent : (a) agent 1 tâche simple : 15 min, (b) agent 2-3 tâches combinées : **45 min** minimum, (c) agent rédaction docs volumineuse : 20 min. Si l'agent dépasse la durée max prévue, envoyer `SendMessage` plutôt que supposer qu'il est bloqué. **Chaque étape des 5 checks de LL-025 doit être refaite** après chaque action intermédiaire. Ne pas « agir sur la base d'un snapshot » vieux de 2 minutes. |

| LL-027 | 2026-04-18 | Processus | **Commandes git cleanup non-idempotentes polluent le terminal** à chaque cycle agent/PR : `git worktree remove` échoue en "not a working tree" après auto-prune du runtime, `git branch --delete` échoue en "not found" après `gh pr merge --delete-branch`, `git worktree remove` échoue en "Permission denied" si l'agent n'a pas encore libéré son lock, `git branch --delete a b c` s'arrête au premier argument absent. Ces erreurs sont inoffensives (l'état final est toujours propre) mais masquent les vraies erreurs dans les logs et font douter le Commanditaire. Signalé par le Commanditaire le 2026-04-18 : « Je vois des shell qui échouent, j'aimerai que tu trouves comment faire pour que ça arrête d'échouer ». | Diagnostic immédiat (identification des 4 causes racines distinctes en <1 min), correctif validé en direct sur PR #35 (pattern fonctionnel dès la 1re application), capitalisation P-023 + LL-027 demandée et acceptée par le Commanditaire (Option A retenue). | J'aurais dû adopter ce pattern dès la première session parallélisée (2026-04-17). La pollution des logs était visible depuis LL-022/024, mais je traitais les erreurs comme « normales » au lieu de les rendre silencieuses. | **Règle permanente — Pattern idempotent systématique sur cleanup git** : toute commande de cleanup git post-merge (remove worktree, delete branch, prune) doit être suffixée par `2>/dev/null \|\| true` pour rester silencieuse sur les cas non-problématiques (ressource déjà supprimée par un autre chemin). Exemples : `git worktree remove -f -f <path> 2>/dev/null \|\| true`, `git branch --delete --force <name> 2>/dev/null \|\| true`, `git worktree prune` (idempotent par nature). Ne JAMAIS utiliser la forme simple sans suppression d'erreur dans un flux agent parallélisé. |

| LL-028 | 2026-04-18 | Processus | **Orchestrateur doit récupérer les livrables d'un agent sans Bash AVANT cleanup du worktree** : les agents DPO et Expert accessibilité de la Fabrique n'ont pas Bash dans leur scope et ne peuvent donc pas commit/push/PR eux-mêmes. Quand l'orchestrateur supprime le worktree après notification (pattern LL-024), les fichiers écrits par l'agent à l'intérieur du worktree sont définitivement perdus. Observé 2026-04-18 : agent DPO TACHE-128 avait produit un registre Art. 30 de 510 lignes dans son worktree isolé, perdu lors du cleanup. L'orchestrateur a dû re-produire le registre lui-même (coût contexte + perte qualité). | Détection immédiate : ls docs/rgpd/ montrait absence du fichier malgré la notification agent réussie. Re-production rapide grâce à la connaissance de la politique de confidentialité v1.0 + AIPD M7 + référentiel ISO 27001 déjà mergés. | Je n'ai pas posé la question « l'agent a-t-il Bash ? » avant de lancer. Le prompt DPO indiquait explicitement « tu as Read/Write/Grep/Glob uniquement — pas de Bash » donc j'aurais dû copier le fichier AVANT cleanup. | **Règle permanente — Récupération obligatoire des livrables avant cleanup** : pour tout agent sans Bash (DPO, Expert accessibilité, Expert UX/UI, Analyste métier, Architecte logiciel/sécurité, Référent qualité — tous les agents « documentation-only »), l'orchestrateur DOIT : (1) identifier dans le rapport final le chemin absolu du livrable produit dans le worktree, (2) `cp <worktree>/<path> <repo_root>/<path>` AVANT tout `git worktree remove`, (3) git add + commit + push + PR depuis une branche de consolidation. Ne JAMAIS supprimer un worktree d'agent sans Bash sans avoir vérifié que le livrable est présent à son emplacement final dans le repo principal. Cette règle s'applique en complément de LL-024 (cleanup systématique). Agents CONCERNÉS : tous ceux ayant `Tools: Read, Write, Grep, Glob` dans leur agent definition (sans Bash). |

| LL-030 | 2026-04-19 | Processus | **Pattern récurrent : agent Dev modifiant CSS/tokens casse les contrastes WCAG AA détectés par l'audit axe-core en CI** — observé 2 fois : PR #59 (T-150 initiale) puis PR feature/p5-chantier-i-ux-refonte-composants (T-143). Les agents ne testent PAS axe-core en local avant push → cycles push-fail-fix-push qui polluent l'historique CI et font douter le Commanditaire. Signalé le 2026-04-19 : « La CI finit en rouge à chaque fois, tu dois progresser et améliorer ton fonctionnement et ta mémoire pour te corriger à chaque fois. » Audit CI a montré 28/30 runs verts (93%), 2 échecs sur une seule branche T-143 en auto-correction, 0 merge avec rouge. Mais la visibilité des rouges intermédiaires donne une mauvaise impression. | Aucune PR n'a été mergée avec CI rouge (audit via `gh run list --json conclusion`). Les agents font bien leur travail d'itération I-009. | Les briefs agents Dev touchant CSS ne mentionnaient PAS l'obligation de lire `docs/accessibilite/audit-axe-core-themes-v1.0.md` ni d'anticiper les zones sensibles (popup dark, options dark). Le Commanditaire a dû signaler le problème. | **Règle permanente — Template brief agent Dev touchant CSS/tokens/HTML DOIT inclure** : (1) lecture obligatoire de `docs/accessibilite/audit-axe-core-themes-v1.0.md` pour connaître les contrastes actuels et les zones historiquement sensibles (popup en thème dark, options en thème dark), (2) vérification que les modifications **préservent** `--sn-color-accent` / `--sn-color-btn-fg` / `--sn-color-warning` dont les valeurs ont été calibrées pour contrast ratio ≥ 4.5:1, (3) si possible lancer `npm run build && npm run test:e2e -- accessibility-themes` avant push (sinon documenter dans le rapport l'impossibilité locale et surveiller activement la CI I-009 pour auto-corriger). Mettre à jour `docs/gouvernance/guide-briefs-agents-fabrique-v1.0.md` §3 avec cette section. Les agents doivent TOUJOURS anticiper l'impact axe-core quand ils touchent au design system. |

_Ce fichier est consolidé à chaque rétrospective de fin de projet. Les enseignements les plus importants sont intégrés dans les fichiers du plugin (.claude/) pour les projets futurs._
